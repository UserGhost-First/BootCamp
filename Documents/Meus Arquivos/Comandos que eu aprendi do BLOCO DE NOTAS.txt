Comando que eu aprendi do BLOCO DE NOTAS FANTASMA


Para abrir os metadados do arquivo:
get-item -path "C:\Users\luizcarvalho\Desktop\Comandos para o terminal VS CODE.txt" -stream *

Para saber o que tem dentro do arquivo:
get-content -path "<caminho do arquivo>"

Agora para remover o item:
remove-item "C:\Users\luizcarvalho\Desktop\Comandos para o terminal VS CODE.txt" -stream <E o nome do path oculto no final>

pathping (Um ping mais detalhado que o tradicional)

Metadados:

$sh     = New-Object -ComObject Shell.Application
$folder = $sh.Namespace('C:\Users\luizcarvalho\Documents')
$file   = $folder.ParseName('Guia rápido.pdf')

0..100 | ForEach-Object {
  $name  = $folder.GetDetailsOf($folder.Items, $_)
  $value = $folder.GetDetailsOf($file, $_)
  if ($value) { "{0,-25}: {1}" -f $name, $value }
}
